# At the end of this code, you should perfectly understand the 'len()' function. This lesson is very brief and I hope you find it helpful

# len() is a built-in Python function that returns the number of items or characters in something like a string or a list.
study_topic = "Studying the 'len ()' functions in python" # The output of this would be 41 when printed below 'print (len(study_topic))
print(len(study_topic)) # It counts the characters in the text.

print (len([1, 2, 3, 4])) # It counts how many items are in the list. OUTPUT = 4;